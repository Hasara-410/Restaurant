# Restaurant E-Menu Application (SoftLien Frontend Intern Test)

## Tech Stack
- Next.js (App Router) + TypeScript
- Tailwind CSS
- JSON Server (provided backend)

## Requirements
- Node.js 18+ (recommended)
- npm

---

## 1) Run Backend (Port 3001)

Open a terminal inside the backend folder (the folder that contains `data-set.json`).

Run:

```bash
npm install
npm run api
